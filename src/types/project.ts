export interface Project {
  title: string;
  description?: string;
  shortDescription: string;
  imagePath?: string;
  technologies: string[];
  urlSite?: string;
  iosUrl?: string;
  playUrl?: string;
}